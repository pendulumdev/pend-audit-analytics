# Treat a dependency's `engines` mismatch as an error, not a warning.
#
# Without this, npm prints EBADENGINE and exits 0. A published CLI cannot
# claim a runtime a dependency refuses. Failing the install is the only
# version of this check that cannot be overlooked.
engine-strict=true
